var file;

function preload(){
	result = loadStrings('file.txt');
}

function setup(){
	noCanvas();
	input = createInput();

	// var ind = floor(random(result.length));
	// text(result[ind], 10,10,80,80);
	// loadStrings('file.txt');
}

function draw(){
}